<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h3>Hey <%= post.name %> you can edit your post below</h3>
    <hr>
    <div class="postCont post">
        <form action="/posts/<%= post.id %>?_method=PATCH" method="post" enctype="multipart/form-data">
            <textarea name="content" class="textarea"><%= post.content %></textarea>
            <br>
            <label for="color"><b>Choose Text-Color for post</b></label>
            <input type="color" name="color" id="color" placeholder="enter the text-color you want...">
            <br>
            <br>
            <p>Upload Media</p>
            <% if (post.media) { %>
                <img src="<%= post.media %>" alt="Post Image" class="postimg">
                <% } %>
                    <br>
                    <input type="file" name="media" accept="image/*">
                    <button type="submit">Submit</button>
        </form>

    </div>
    <hr>
</body>

</html>